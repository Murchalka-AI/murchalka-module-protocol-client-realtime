# Client Realtime Protocol

Authenticated loopback WebSocket bridge for universal Murchalka clients. It exposes no product state and invokes only bound Auth, Agent Runtime, and Agent UI capabilities.

Tags in canonical `vX.Y.Z` form publish signed immutable bundles for all supported RIDs.

